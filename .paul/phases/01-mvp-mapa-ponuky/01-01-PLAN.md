---
phase: 01-mvp-mapa-ponuky
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/lib/types.ts, src/lib/geo.ts, src/lib/providers/*, src/lib/db/*, src/lib/sync.ts, src/lib/*.test.ts, vitest.config.ts]
autonomous: true
---

<objective>
## Goal
Next.js scaffold s dátovou vrstvou: typy, geo utility (bod v polygóne), vymeniteľný `PropertyProvider` s mock dátami z talianskeho pobrežia, repozitár nad Supabase (service_role, len server) s in-memory fallbackom a sync logika, ktorá rozpozná nové inzeráty a zmeny cien.

## Purpose
Všetky ďalšie fázy (mapa, notifikácie, AI) stoja na jednej dátovej vrstve. Mock provider umožní vyvíjať bez vybraného dátového poskytovateľa (PRD §7).

## Output
`src/lib/**`, unit testy, `vitest.config.ts`, závislosti v `package.json`.
</objective>

<context>
@.paul/PROJECT.md
@.paul/Adelka_Home_Finder_PRD.md (§5.3 schéma)
</context>

<acceptance_criteria>

## AC-1: Build a testy
```gherkin
Given čerstvý checkout
When spustím npm run build, npm run lint a npm test
Then všetko prejde bez chýb
```

## AC-2: Bod v polygóne
```gherkin
Given GeoJSON polygón oblasti (aj s dierami / MultiPolygon)
When testujem súradnice inzerátu
Then vnútri = true, mimo = false
```

## AC-3: Sync rozpozná nové a zmenené ceny
```gherkin
Given repozitár s existujúcimi inzerátmi
When provider vráti nový inzerát a jeden existujúci s inou cenou
Then sync ho vloží/aktualizuje a vráti zoznam nových a zmenených (so starou cenou)
And last_seen_at sa aktualizuje, first_seen_at ostane
```

## AC-4: Fallback bez DB
```gherkin
Given chýbajú Supabase env premenné
When appka číta dáta
Then použije in-memory repozitár a nespadne
```

## AC-5: Service key nikdy na klientovi
```gherkin
Given modul so Supabase klientom
When by sa importoval v klientskom komponente
Then build zlyhá (import 'server-only')
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Závislosti, typy, geo utility, mock provider</name>
  <files>package.json, src/lib/types.ts, src/lib/geo.ts, src/lib/providers/index.ts, src/lib/providers/mock.ts</files>
  <action>
    - npm i @supabase/supabase-js server-only leaflet react-leaflet; -D @types/leaflet vitest
    - types.ts: Property, ProviderListing, SearchArea, SavedListing, LocationNote, AreaGeometry (GeoJSON Polygon/MultiPolygon)
    - geo.ts: pointInPolygon (ray casting, diery), pointInArea, normalizeGeometry, bboxOf
    - PropertyProvider rozhranie { id, label, fetchListings() }, getProvider() podľa PROPERTY_PROVIDER (default "mock")
    - mock.ts: deterministicky ~36 inzerátov v 18 pobrežných mestách (Ligúria, Toskánsko, Apúlia), seedovaný PRNG, fotky z picsum.photos/seed
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>AC-2 pripravené</done>
</task>

<task type="auto">
  <name>Task 2: Repozitár (Supabase + memory) a sync</name>
  <files>src/lib/db/supabase.ts, src/lib/db/repo.ts, src/lib/db/supabase-repo.ts, src/lib/db/memory-repo.ts, src/lib/sync.ts</files>
  <action>
    - supabase.ts: `import 'server-only'`, createClient so SUPABASE_SERVICE_ROLE_KEY, persistSession false; isDbConfigured()
    - Repository rozhranie: properties (list, get, upsert), search areas (list, create, delete), saved (list, save, updateNote, remove), location notes (get, upsert)
    - MemoryRepository cez globalThis singleton (prežije HMR)
    - sync.ts: diffListings (čistá funkcia) + syncFromProvider(repo, provider) → { inserted, priceChanged, total }
    - ensureSeeded(): ak je tabuľka properties prázdna, spusti sync (prvé spustenie)
    - Avoid: meniť DB schému (je už spustená v Supabase)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>AC-3, AC-4, AC-5</done>
</task>

<task type="auto">
  <name>Task 3: Unit testy</name>
  <files>vitest.config.ts, src/lib/geo.test.ts, src/lib/sync.test.ts, package.json (script test)</files>
  <action>Vitest s aliasom @; testy pre pointInPolygon (vnútri, mimo, diera, MultiPolygon) a diffListings/syncFromProvider nad MemoryRepository.</action>
  <verify>npm test && npm run lint && npm run build</verify>
  <done>AC-1, AC-2, AC-3</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- .paul/Adelka_Home_Finder_PRD.md
- Supabase schéma (bez migrácií v tejto fáze)

## SCOPE LIMITS
- Žiadne UI okrem scaffoldu (to je 01-02)
- Žiadny reálny dátový poskytovateľ
</boundaries>

<verification>
- [ ] npm test
- [ ] npm run lint
- [ ] npm run build
</verification>

<success_criteria>
- Všetky úlohy hotové, overenia prešli
</success_criteria>

<output>
.paul/phases/01-mvp-mapa-ponuky/01-01-SUMMARY.md
</output>
