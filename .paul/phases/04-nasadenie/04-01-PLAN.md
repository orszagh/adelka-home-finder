---
phase: 04-nasadenie
plan: 01
type: execute
wave: 1
depends_on: ["03-01"]
files_modified: [src/proxy.ts, src/lib/auth.ts, src/app/prihlasenie/*, README.md, .env.example, .gitattributes, .gitignore, vercel.json]
autonomous: true
---

<objective>
## Goal
Appka je pripravená na produkciu: prístup len s heslom, zdokumentované premenné, nasadenie a DNS postup, kód na GitHube (Vercel nasadí automaticky).

## Purpose
Server actions a AI endpointy sú dosiahnuteľné priamymi POST požiadavkami; bez ochrany by ktokoľvek menil dáta cez service_role a míňal AI kredit.
</objective>

<acceptance_criteria>

## AC-1: Bezpečné predvolené správanie
```gherkin
Given produkcia bez APP_PASSWORD
Then každá stránka vráti 503 s návodom, nič nie je prístupné
```

## AC-2: Prihlásenie
```gherkin
Given APP_PASSWORD
When neprihlásené zariadenie otvorí stránku
Then presmeruje na /prihlasenie?next=… ; API a POST vrátia 401
When Adelka zadá správne heslo
Then dostane httpOnly cookie na 1 rok a vráti sa na pôvodnú stránku (len lokálne cesty)
And zlé heslo zobrazí chybu so spomalením
```

## AC-3: Výnimky
```gherkin
Then /api/cron/* (vlastný CRON_SECRET) a /mock-photo/* (obrázky v emailoch) fungujú bez prihlásenia
```

## AC-4: Dokumentácia a nasadenie
```gherkin
Then README popisuje premenné, Vercel, Resend a DNS na Webglobe; kód je pushnutý na GitHub
```

</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Proxy + prihlásenie</name>
  <files>src/proxy.ts, src/lib/auth.ts, src/app/prihlasenie/*</files>
  <action>HMAC token z APP_PASSWORD (Web Crypto), cookie adelka_session, safeNextPath proti open redirectu.</action>
  <verify>unit testy + curl na prod builde</verify>
  <done>AC-1, AC-2, AC-3</done>
</task>
<task type="auto">
  <name>Task 2: Dokumentácia, konfigurácia, push</name>
  <files>README.md, .env.example, .gitattributes, .gitignore, vercel.json</files>
  <action>README po slovensky, .env.example, LF konce riadkov, framework nextjs vo vercel.json, git push origin main.</action>
  <verify>npm test, lint, build; git push</verify>
  <done>AC-4</done>
</task>
</tasks>

<output>
.paul/phases/04-nasadenie/04-01-SUMMARY.md
</output>
