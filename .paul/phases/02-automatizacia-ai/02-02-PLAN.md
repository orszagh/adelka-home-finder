---
phase: 02-automatizacia-ai
plan: 02
type: execute
wave: 1
depends_on: ["01-02"]
files_modified: [src/lib/ai/claude.ts, src/lib/ai/location.ts, src/app/api/location-summary/route.ts, src/components/LocationSummary.tsx, src/app/inzerat/[id]/page.tsx]
autonomous: true
---

<objective>
## Goal
AI prehľad lokality na jedno ťuknutie z detailu inzerátu: celkový dojem, infraštruktúra a dostupnosť, klimatické riziká, riziko spodnej vody, zaujímavosti. Výsledok sa cachuje v `location_notes`.

## Purpose
PRD cieľ 4: Adelka posúdi lokalitu bez vlastného googlenia.

## Output
Claude klient (claude-opus-5, štruktúrovaný výstup Zod, server-side fallback), route handler, UI komponent.
</objective>

<acceptance_criteria>

## AC-1: Prehľad na vyžiadanie
```gherkin
Given detail inzerátu v meste bez uloženého prehľadu
When Adelka ťukne "Zobraziť prehľad lokality"
Then appka zavolá Claude API, uloží výsledok do location_notes a zobrazí ho po slovensky v sekciách
```

## AC-2: Cache
```gherkin
Given prehľad pre mesto už existuje
When Adelka otvorí iný inzerát v tom istom meste
Then prehľad sa zobrazí hneď bez volania AI, s dátumom vytvorenia a možnosťou obnoviť
```

## AC-3: Chyby a poctivosť
```gherkin
Given chýba ANTHROPIC_API_KEY alebo AI odmietne/zlyhá
Then UI zobrazí zrozumiteľnú správu, nič nespadne
And prehľad vždy upozorní, že ide o orientačné informácie na overenie (ISPRA IdroGEO, obec)
```

## AC-4: Bez zneužitia
```gherkin
Given požiadavka na API
Then mesto a región sa berú z inzerátu v DB podľa propertyId, nie z voľného textu
```

</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Claude klient + generovanie prehľadu</name>
  <files>src/lib/ai/claude.ts, src/lib/ai/location.ts</files>
  <action>client.beta.messages.parse s zodOutputFormat, model claude-opus-5 (ANTHROPIC_MODEL override), fallbacks "default", ošetrenie refusal/max_tokens, mapovanie na stĺpce location_notes.</action>
  <verify>npx tsc --noEmit, unit test mapovania</verify>
  <done>AC-1, AC-3</done>
</task>
<task type="auto">
  <name>Task 2: API route + UI</name>
  <files>src/app/api/location-summary/route.ts, src/components/LocationSummary.tsx, src/app/inzerat/[id]/page.tsx</files>
  <action>POST {propertyId, refresh}, maxDuration 120; komponent s načítaním, chybou a obnovením.</action>
  <verify>prehliadač: chybový stav bez kľúča; build</verify>
  <done>AC-2, AC-3, AC-4</done>
</task>
</tasks>

<boundaries>
## SCOPE LIMITS
- Bez web search (cena, zložitosť); prehľad je z vedomostí modelu s upozornením
</boundaries>

<output>
.paul/phases/02-automatizacia-ai/02-02-SUMMARY.md
</output>
